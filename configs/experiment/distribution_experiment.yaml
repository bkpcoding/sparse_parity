defaults:
  - _self_
  - ../base_config@_global_

comparison_mode: "both"  # Options: "uniform", "zipf", "both"
lr: 1e-3
weight_decay: 0
use_wandb: false

distribution_type: "zipf"  # Default, will be overridden in experiment
alpha: 2.0  # Zipf distribution parameter
offset: 0  # Offset for Zipf distribution
