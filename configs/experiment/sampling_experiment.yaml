defaults:
  - _self_
  - ../base_config@_global_

comparison_mode: "temperature"  # Options: uniform, temperature, both
use_wandb: false
lr: 0.001
weight_decay: 0.0
temperature: 3.0  # Initial temperature for temperature sampling
distribution_type: "zipf"
alpha: 1.0
offset: 0

n_ensembles: 1
plot_losses: false